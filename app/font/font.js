import { Alkatra, Caveat } from 'next/font/google';

export const alkatra = Alkatra({
  weight: ['400'],
  subsets: ['latin'],
  display: 'swap' ,
  variable: '--font-alkatra', 
});

export const caveat = Caveat({
  weight: ['400', '700'],
  subsets: ['latin'],
  display: 'swap',
  variable: '--font-caveat',
});


